package com.example.medy.patient.internal.service;

/** Every user-facing error message thrown by {@link PatientService}, in one place. */
final class PatientMessages {

    static final String PATIENT_NOT_FOUND = "Patient %s not found";

    private PatientMessages() {
    }
}
