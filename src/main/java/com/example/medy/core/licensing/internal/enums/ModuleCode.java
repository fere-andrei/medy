package com.example.medy.core.licensing.internal.enums;

/**
 * One value per gate-able module. Add a new constant only once that module's
 * controller actually exists — this enum isn't meant to pre-declare the full
 * product roadmap.
 */
public enum ModuleCode {
    PATIENT_MANAGEMENT
}
